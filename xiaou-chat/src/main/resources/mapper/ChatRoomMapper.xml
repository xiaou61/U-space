<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaou.chat.mapper.ChatRoomMapper">

    <resultMap id="BaseResultMap" type="com.xiaou.chat.domain.ChatRoom">
        <id column="id" property="id"/>
        <result column="room_name" property="roomName"/>
        <result column="room_type" property="roomType"/>
        <result column="description" property="description"/>
        <result column="max_users" property="maxUsers"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, room_name, room_type, description, max_users, status, create_time, update_time
    </sql>

    <select id="selectById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM chat_rooms
        WHERE id = #{id}
    </select>

    <select id="selectOfficialRoom" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM chat_rooms
        WHERE room_type = 1 AND status = 1
        LIMIT 1
    </select>

</mapper>
