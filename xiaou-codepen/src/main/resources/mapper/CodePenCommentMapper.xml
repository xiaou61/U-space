<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaou.codepen.mapper.CodePenCommentMapper">

    <resultMap id="BaseResultMap" type="com.xiaou.codepen.domain.CodePenComment">
        <id column="id" property="id"/>
        <result column="pen_id" property="penId"/>
        <result column="user_id" property="userId"/>
        <result column="content" property="content"/>
        <result column="parent_id" property="parentId"/>
        <result column="reply_to_user_id" property="replyToUserId"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="com.xiaou.codepen.domain.CodePenComment" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO code_pen_comment (pen_id, user_id, content, parent_id, reply_to_user_id, status, create_time)
        VALUES (#{penId}, #{userId}, #{content}, #{parentId}, #{replyToUserId}, 1, NOW())
    </insert>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT id, pen_id, user_id, content, parent_id, reply_to_user_id, status, create_time, update_time
        FROM code_pen_comment
        WHERE id = #{id}
    </select>

    <select id="selectByPenId" resultMap="BaseResultMap">
        SELECT id, pen_id, user_id, content, parent_id, reply_to_user_id, status, create_time, update_time
        FROM code_pen_comment
        WHERE pen_id = #{penId}
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY create_time DESC
    </select>

    <update id="updateStatus">
        UPDATE code_pen_comment
        SET status = #{status}, update_time = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM code_pen_comment
        WHERE id = #{id}
    </delete>

</mapper>

