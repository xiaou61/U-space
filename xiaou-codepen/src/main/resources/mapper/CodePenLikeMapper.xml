<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaou.codepen.mapper.CodePenLikeMapper">

    <resultMap id="BaseResultMap" type="com.xiaou.codepen.domain.CodePenLike">
        <id column="id" property="id"/>
        <result column="pen_id" property="penId"/>
        <result column="user_id" property="userId"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <insert id="insert" parameterType="com.xiaou.codepen.domain.CodePenLike">
        INSERT INTO code_pen_like (pen_id, user_id, create_time)
        VALUES (#{penId}, #{userId}, NOW())
    </insert>

    <delete id="delete">
        DELETE FROM code_pen_like
        WHERE pen_id = #{penId} AND user_id = #{userId}
    </delete>

    <select id="selectByPenIdAndUserId" resultMap="BaseResultMap">
        SELECT id, pen_id, user_id, create_time
        FROM code_pen_like
        WHERE pen_id = #{penId} AND user_id = #{userId}
    </select>

    <select id="selectCountByPenId" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM code_pen_like
        WHERE pen_id = #{penId}
    </select>

</mapper>

